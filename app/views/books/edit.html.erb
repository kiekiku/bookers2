<% if @book_detail.errors.any? %>
  <%= @book_detail.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @book_detail.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<h1>Editing Book</h1>
<%= form_with model: @book_detail do |f| %>
 <label for="inputTitle">Title</label>
 <%= f.text_field :title, autofocus: true, id:"inputTitle" %>
 <label for="inputOpinion">Opinion</label>
 <%= f.text_area:body, autofocus: true, id:"inputOpinion" %>
 <%= f.submit "Update Book" %>
<%end%>

<%= link_to "Show", book_path %>
|
<%= link_to "Back", books_path %>
